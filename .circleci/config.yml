version: 2

jobs:
  hello:
    docker:
      - image: debian:stretch-slim
    steps:
      - run: echo 'Hello, world'


workflows:
  version: 2
  cron:
    triggers:
      - schedule:
          cron: '*/5 * * * *'
    jobs:
      - hello
